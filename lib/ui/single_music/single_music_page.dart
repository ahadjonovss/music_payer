import 'package:flutter/material.dart';

class SingleMusicPage extends StatelessWidget {
  const SingleMusicPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
